import { PipePersonalPipe } from './pipe-personal.pipe';

describe('PipePersonalPipe', () => {
  it('create an instance', () => {
    const pipe = new PipePersonalPipe();
    expect(pipe).toBeTruthy();
  });
});
